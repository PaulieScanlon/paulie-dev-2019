---
navigationLabel: writing
---

import { WritingCard } from '../components/writing-card'

# writing

Here's a collection of articles I've written that have been officially published 🕺

<Divider />

<SourceList filter="writing">
  {(writing) => (
    <Grid
      sx={{
        gap: 4
      }}
    >
      {writing
        .filter((edge) => !edge.node.frontmatter.isPrivate)
        .map((edge, index) => {
          const {
            frontmatter: { title, featuredImage, tags, date, url, misc, author },
            excerpt,
      
          } = edge.node
          return (
            <WritingCard
              key={index}
              title={title}
              featuredImage={featuredImage}
              tags={tags}
              date={date}
              excerpt={excerpt}
              url={url}
              misc={misc}
              author={author}
            />
          )
        })}
    </Grid>
)}

</SourceList>
